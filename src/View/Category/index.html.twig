{% extends 'layout.html.twig' %}

{% block stylesheet  %}
	{{ parent() }}
	<link rel="stylesheet" href="/assets/css/category.css">
{% endblock %}

{% block content %}

	<ul>
		<div class="titleCategory">
			<p>
				{{ category.name }}
			</p>
		</div>

		{% include 'Category/_search_bar.html.twig' %}

		{% for gameInfo in gameInfos %}
			<li class="gameCategory">
				<a href='descriptionGame?id={{ gameInfo.game_id }}'<div class="titleGame">
					<p>
						{{ gameInfo.name }}
					</p>
				</div>
				</a>

				<div class="categories">
					<p>Action</p>
					<p>Plateformer</p>
					<p>RPG</p>
		
				</div>
				
				<div class="imgGameCategory">
					<img src="/assets/images/pictures_game/covers/{{ gameInfo.picture }}" alt={{ gameInfo.name }}>
				</div>

				<div class="Mylist_opinion">
					<p>
						recommandations (positive/negative)
					</p>
					<button class="buttonGameCategory" type="submit">Ajouter à ma liste</button>
				</div>
			</li>
		{% endfor %}
	</ul>

{% endblock %}
